import { h } from '../ionic.core.js';

export { m as PLATFORMS_MAP, n as getPlatforms, l as isPlatform, o as setupPlatforms } from './chunk-03ecffc3.js';
export { createSwipeBackGesture } from './swipe-back.js';
import './gesture.js';
import './chunk-647a6bad.js';

function setupConfig(config) {
    const win = window;
    const Ionic = win.Ionic;
    if (Ionic && Ionic.config && Ionic.config.constructor.name !== 'Object') {
        console.error('ionic config was already initialized');
        return;
    }
    win.Ionic = win.Ionic || {};
    win.Ionic.config = Object.assign({}, win.Ionic.config, config);
    return win.Ionic.config;
}

export { setupConfig };
