import { ComponentFactoryResolver, ElementRef, Injector, ViewContainerRef } from '@angular/core';
import { AngularDelegate } from '../../providers/angular-delegate';
import { IonRouterOutlet } from './ion-router-outlet';
export declare class TabDelegate {
    outlet?: IonRouterOutlet;
    private nativeEl;
    constructor(elementRef: ElementRef, resolver: ComponentFactoryResolver, injector: Injector, angularDelegate: AngularDelegate, location: ViewContainerRef);
    readonly tab: string;
    getLastUrl(): string | undefined;
    onNavChanged(): Promise<void>;
}
