import { ComponentRef, NgZone } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
import { NavController, NavDirection } from '../../providers/nav-controller';
export declare class StackController {
    private stack;
    private containerEl;
    private router;
    private navCtrl;
    private zone;
    private viewsSnapshot;
    private views;
    private runningTransition?;
    private skipTransition;
    constructor(stack: boolean, containerEl: HTMLIonRouterOutletElement, router: Router, navCtrl: NavController, zone: NgZone);
    createView(enteringRef: ComponentRef<any>, route: ActivatedRoute): RouteView;
    getExistingView(activatedRoute: ActivatedRoute): RouteView | undefined;
    setActive(enteringView: RouteView, direction: NavDirection, animated: boolean): Promise<void>;
    canGoBack(deep: number): boolean;
    pop(deep: number): void;
    startBackTransition(): void;
    endBackTransition(shouldComplete: boolean): void;
    private insertView;
    private cleanup;
    getActive(): RouteView | undefined;
    private transition;
    private getUrl;
}
export interface RouteView {
    url: string;
    element: HTMLElement;
    ref: ComponentRef<any>;
    savedData?: any;
}
